export enum list_messageName_enum {
  patch_fullname = 'patch_fullname.approval',
  leave_request = 'leave_request.approval',
}

// list các messageName gửi cho approval service dùng để bắn sự kiện thành công hay thất bại
